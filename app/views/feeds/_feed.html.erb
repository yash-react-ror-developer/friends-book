<%= feed.title %>&nbsp<% if current_user.id == feed.user_id %>|<%= link_to 'Edit', edit_feed_path(feed)%><%end%>&nbsp|&nbsp<%= link_to 'Show', feed_path(feed)%>&nbsp<% if current_user.id == feed.user_id %>|&nbsp<%= link_to 'Delete', feed_path(feed), method: :Delete%><%end%>&nbsp|&nbsp<% unless current_user.userbookmarks.find_by_feed_id(feed.id) %> <%= link_to 'Mark as Bookmark', feed_book_marked_path(feed), method: :patch, remote: true%> <%else%> <%= link_to 'Marked', "#"%><%end%>